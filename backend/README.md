# Backend

Chưa có logic server nào — trang portfolio hiện tại hoàn toàn tĩnh (static HTML/CSS/JS), không cần backend để chạy.

Thư mục này để dành cho sau này nếu cần, ví dụ:
- API xử lý form liên hệ
- Endpoint để chỉnh sửa nội dung (thành tích, hoạt động...) mà không cần sửa trực tiếp HTML
